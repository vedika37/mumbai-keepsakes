// import { useRouter } from 'next/router';

const PinDetail = () => {
  // const router = useRouter();
  // const { id } = router.query; // Use id from the URL

  return (
    <div>
      <h1>Pin Detail for ID: {"id"}</h1>
      {/* Fetch and display pin details from Firebase */}
    </div>
  );
};

export default PinDetail;
